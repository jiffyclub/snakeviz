language: python
python:
  - "2.6"
  - "2.7"
  - "3.4"
# command to install dependencies
install:
  - pip install tornado
  - pip install jinja2
  - python setup.py install
# command to run tests
script:
  - nosetests
