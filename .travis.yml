language: python
python:
  - "2.7"
  - "3.4"
# command to install dependencies
install:
  - pip install tornado
  - pip install jinja2
  - pip install pyyaml
  - python setup.py install
# command to run tests
script:
  - nosetests
